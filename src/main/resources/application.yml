spring:
  datasource:
    url: jdbc:postgresql://postgres-container:5432/paymentdb
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true